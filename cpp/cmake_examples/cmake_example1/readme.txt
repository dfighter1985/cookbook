This is a simple CMake single source file example.
It creates a Makefile / project file for a single executable from a single source file.

Usage:
mkdir build
cd build
cmake ../src
cmake --build .
cmake --build . --target install
