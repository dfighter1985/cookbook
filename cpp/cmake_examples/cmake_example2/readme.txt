This is a simple CMake single source and single header file example.
It creates a Makefile / project file for a single executable from a single source file and single header file.

Usage:
mkdir build
cd build
cmake ../src
cmake --build .
cmake --build . --target install
